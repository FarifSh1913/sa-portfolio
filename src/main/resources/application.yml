server:
  port: 8080
spring:
  application:
    name: sa-portfolio
management:
  endpoints:
    web:
      exposure:
        include: health,info
ai-agent:
  api-url: ${VITE_AI_AGENT_API_URL:http://localhost:8082}
